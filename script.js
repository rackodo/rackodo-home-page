let titles = [
	"rackodo",
]

let titleCounter = 0;

// setInterval(() => {
// 	document.getElementById('header').innerHTML = titles[titleCounter]
// 	titleCounter++;

// 	if (titleCounter > titles.length - 1) {
// 		titleCounter = 0;
// 	}
// }, 500);